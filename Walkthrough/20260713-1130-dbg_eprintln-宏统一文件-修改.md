# Walkthrough: dbg_eprintln! 宏定义统一文件审计

## 1. 改动摘要

**本次任务无代码改动**——经审计，`dbg_eprintln!` 宏定义已位于专用 .rs 文件，符合"统一到一个 .rs 文件"要求。

审计对象：

| 项 | 现状 |
|----|------|
| 宏定义文件 | `src-tauri/src/services/debug_log.rs` |
| 宏定义行号 | `debug_log.rs:32` (`macro_rules! dbg_eprintln`) |
| `#[macro_export]` | ✅ 存在（`debug_log.rs:31`） |
| 模块声明 | ✅ `services/mod.rs:6` `pub mod debug_log;` |
| 同类 debug 宏 | 仅此一个，无 `dbg_println!` / `dbg_eprintln_if!` 等分散定义 |
| 调用点数量 | 240 处（跨 16 文件，含 `debug_log.rs` 自身 4 处注释/文档示例引用） |
| 用户描述预期 | "约 245 处" — 与实际 240 处基本一致 |

文件顶部包含完整清除步骤说明与约定（"本文件是项目调试日志的统一收口点"），并预留未来迁移至 `log` / `tracing` 的路径。

## 2. 改动动机

用户决策 L1："debug 日志代码统一到一个 .rs 文件中"——不迁移到 `tracing`，沿用现有 `dbg_eprintln!` 宏，确保宏定义集中收口到单一 .rs 文件，所有调用通过该文件导入。

本次任务**只整理宏定义位置**，不修改任何调用点（240 处 `dbg_eprintln!` 调用保持不变）。

经审计，宏定义已满足"统一到一个 .rs 文件"的硬要求，无需迁移。

## 3. 约束合规审计表

| 约束 | 合规 | 说明 |
|------|------|------|
| C1 Frontend Render-Only | √ | 本任务仅涉及后端 Rust 宏定义位置审计，无前端改动 |
| C2 Zero-Fallback Errors | √ | 宏体仅包装 `eprintln!`，不吞错；未引入静默回退；`#[macro_export]` 暴露至 crate 根供全局调用 |
| C3 Responsiveness | √ | `eprintln!` 同步写 stderr，单条精简（约定 1~2 行），非热路径阻塞；长输出约定改文件落盘 |
| C4 AI UI Isolation | √ | 不涉及 AI 渲染层 |
| C5 Mobile Frontend Independence | √ | 不涉及前端，PC/移动端共享同一 Rust 后端宏定义 |
| C6 Project Cache Location | √ | 宏不写缓存，仅 stderr 输出 |
| C7 PC/Android Coverage | √ | 后端宏两端共享，无单端后门 |

## 4. 验收记录

### 构建验证

本次无代码改动，跳过 `cargo build` / `cargo clippy`（无 diff 无需重新编译；当前 git 工作树状态与本任务前一致）。

如需复验，可在 `src-tauri/` 下执行：
```powershell
cargo build 2>&1 | Select-Object -Last 30
cargo clippy --all-targets 2>&1 | Select-Object -Last 30
```

### Grep 验证调用点未变

```
Grep pattern: dbg_eprintln!
Path: d:\data\Night Voyage\src-tauri\src
Output mode: count
```

结果（共 240 处，16 文件）：

| 文件 | 计数 |
|------|------|
| `lib.rs` | 1 |
| `db/mod.rs` | 15 |
| `commands/characters.rs` | 1 |
| `network/mod.rs` | 43 |
| `repositories/preset_repository.rs` | 2 |
| `commands/conversations.rs` | 33 |
| `validators/preset_validator.rs` | 2 |
| `commands/providers.rs` | 24 |
| `services/chat_service.rs` | 39 |
| `commands/rooms.rs` | 3 |
| `services/debug_log.rs` | 4（注释/文档示例） |
| `services/mem0_snapshot.rs` | 1 |
| `services/memory_providers/mem0_rs.rs` | 2 |
| `services/prompt_compiler.rs` | 38 |
| `services/stream_processor.rs` | 30 |
| `services/structured_output_parser.rs` | 2 |
| **总计** | **240** |

### 宏定义位置验证

```
Grep pattern: macro_rules!\s+dbg_eprintln
Path: d:\data\Night Voyage\src-tauri\src
```

结果：仅 1 处命中——`src-tauri/src/services/debug_log.rs:32`。

```
Grep pattern: macro_rules!\s+(eprintln|println|debug_|log_)
Path: d:\data\Night Voyage\src-tauri\src
```

结果：无命中——确认无其他散落的同类 debug 宏。

## 5. 已知限制或后续待办

1. **`debug_log.rs` 当前路径为 `services/`**：用户任务描述推荐过 `utils/debug_log.rs` 或 `src-tauri/src/debug_log.rs`，但当前 `services/debug_log.rs` 已是专用独立文件，路径合理（debug_log 本身属于"调试服务"语义范畴）。如未来想统一调整至 `utils/`，需同步修改 `services/mod.rs` 与 `Cargo.toml` 中可能的引用——本次不做调整。

2. **未来迁移至 `tracing` 的路径**：文件顶部已预留清除步骤（删除本文件 → 移除 `pub mod debug_log;` → 全局替换 `dbg_eprintln!` 为 `log::info!` / `log::warn!` / `log::error!`）。本次任务不执行该迁移。

3. **本任务未执行 git commit**：按步骤 7 要求，提交由主 agent 统一处理。
