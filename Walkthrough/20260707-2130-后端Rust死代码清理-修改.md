# 后端 Rust 死代码清理

## 1. 改动摘要

清理 `src-tauri/src/` 下的死代码，消除 cargo build 警告。共修改 12 个文件，删除 49 项死代码（31 条第一批 + 18 条第二批）。

### 修改文件清单

| 文件 | 删除项数 | 说明 |
|------|---------|------|
| `src-tauri/src/models/mod.rs` | 12 | 遗留结构体：Conversation、Message、StreamChunk、StreamError、PresetCompilePreviewMessage/Params/Preview、MessageContentPartRecord、MessageToolCallRecord、AgentBinding/Run/Draft |
| `src-tauri/src/validators/preset_validator.rs` | 11 | 8 个 PresetValidator::normalize_* 转发方法 + normalize_required_from_row_impl + serialize_optional_string_array_impl + parse_optional_json_string_array_impl |
| `src-tauri/src/repositories/message_repository.rs` | 4 | reset_response_artifacts_tx、reset_response_artifacts、ensure_hidden_message_part、append_hidden_part_text |
| `src-tauri/src/repositories/llm_retry_snapshot_repository.rs` | 1+导入 | validate_snapshot_rules + 清理未使用导入 validate_output_text_with_retry_snapshot |
| `src-tauri/src/repositories/round_repository.rs` | 1 | mark_completed |
| `src-tauri/src/services/chat_service.rs` | 1 | build_openai_url（重复定义，plot_summaries.rs 有存活版本） |
| `src-tauri/src/services/prompt_compiler.rs` | 5 | validate_output_text、validate_output_text_with_validators、load_character_system_message、load_character_base_block、DEFAULT_RETRIEVED_DETAIL_TOP_K 常量 |
| `src-tauri/src/services/stream_processor.rs` | 1 | handle_stream_completion |
| `src-tauri/src/services/chat/capability_guard.rs` | 1 | CONV_TYPE_SINGLE 常量 |
| `src-tauri/src/commands/exchange.rs` | 1+参数 | Manager 未使用导入 + character_cards_export 的 app→_app |
| `src-tauri/src/commands/conversations.rs` | 1 | row_to_conversation_list_item |
| `src-tauri/src/llm/mod.rs` | 18 | LLM/嵌入/向量存储抽象层：LlmResult、VectorStoreResult、LlmStreamEventKind、LlmToolUsePayload、LlmStreamEvent、ChatRequest、ChatChunk、ChatStreamHandle、EmbeddingRequest、EmbeddingVector、VectorPoint、VectorQuery、VectorMatch、LlmError、VectorStoreError、ModelProviderGateway、EmbeddingGateway、VectorStore |

## 2. 改动动机

- 消除 cargo build 的 61 条死代码警告，提升代码可维护性
- 向量化代码（VectorStore/Embedding 相关）已被 mem0 代替，清理遗留抽象层
- LLM 网关抽象层（ModelProviderGateway/EmbeddingGateway/VectorStore trait）从未被实现，清理
- 遗留结构体（Conversation/Message 等）已被实际生产使用的结构体（ConversationListItem/UiMessage 等）取代

## 3. 约束合规审计表

| 约束 | 合规 | 说明 |
|------|------|------|
| C1 Frontend Render-Only | √ | 仅清理后端 Rust 代码，未触碰前端 |
| C2 Zero-Fallback Errors | √ | 删除的是死代码，未改变任何错误处理逻辑 |
| C3 Responsiveness | √ | 未改变任何运行时行为 |
| C4 AI UI Isolation | √ | 未涉及 AI UI 层 |
| C5 Mobile Frontend Independence | √ | 仅修改 src-tauri/，未触碰 src-mobile/ |
| C6 Project Cache Location | √ | 未涉及缓存路径 |
| C7 PC/Android Coverage | √ | 后端共享代码清理，两端均受益 |

## 4. 验收记录

### 构建验证

```
cargo build（在 src-tauri/ 下）
exit code: 0
警告数: 11（清理前 61 → 清理后 11，减少 50 条）
```

剩余 11 条警告全部属于"不清理"范围：
- network/mod.rs: ClientHandle/RoomServer 死字段（不清理）
- llm_retry_snapshot_repository.rs: RetrySnapshotRecord 死字段（不清理）
- prompt_compiler.rs: AgentDirectorPlaceholder 变体（保留，未来功能占位）
- prompt_compiler.rs: PresetCompilePreviewData 结构体（保留，关联测试专用函数）
- prompt_compiler.rs: CompiledOutputValidator.source 字段（结构体死字段，不清理）
- prompt_compiler.rs: compile_chat_messages/compile_preset_preview_data/build_preview_template_render_context（测试专用函数，不清理）
- provider_adapter.rs: adapt_prompt_compile_result_to_openai_messages（测试专用函数，不清理）
- stream_processor.rs: StreamResponseData 死字段（不清理）

### 测试验证

```
cargo test --lib services::chat
20 passed; 0 failed; 0 ignored
```

```
cargo test --lib
68 passed; 4 failed; 0 ignored
```

4 个失败测试经 `git stash` 验证为**预存失败**（在原始代码上也失败），与本次清理无关：
1. `openai_structured_json_request_sets_json_schema_format` - assertion `strict: true` vs `false`（provider_adapter.rs，未修改）
2. `preview_filters_template_blocks_by_normalized_disabled_type` - SQL 错误 "no such column: context_included_keys"（迁移缺失）
3. `preview_applies_provider_override_penalties_and_filters_disabled_blocks` - 同上 SQL 错误
4. `preset_compiler_extracts_prefill_and_output_validators` - 同上 SQL 错误

### mem0 代码保护确认

未触碰以下 mem0 相关文件：
- `src-tauri/src/services/mem0_snapshot.rs`
- `src-tauri/src/services/memory_providers.rs`
- `src-tauri/src/services/memory_providers/mem0_rs.rs`
- `src-tauri/src/services/chat/capability_guard.rs` 中 mem0_snapshot 导入保留

## 5. 已知限制或后续待办

- 4 个预存测试失败需单独修复（数据库迁移缺失 + assertion 值不匹配）
- 剩余 11 条警告属于"不清理"范围，如需进一步清理需单独评估
- `CompiledOutputValidator.source` 字段因删除 `validate_output_text_with_validators` 而变为死字段，但按任务要求"结构体死字段本次不删"
