# 前端死代码清理与 tsc 错误修复 Walkthrough

**文件名**：20260707-2145-前端死代码清理与tsc错误修复-修改.md
**修改类型**：修改
**关联 Walkthrough**：
- `20260707-2130-后端Rust死代码清理-修改.md`（后端清理，本次同步进行）

## 1. 改动摘要

### 1.1 删除的文件

- **`src/components/MobileView.tsx`**（330 行）
  - 废弃组件，全项目 `src/` 目录 Grep 确认无任何 `import` 引用
  - 该组件是早期 PC 前端目录中的移动端视图残留，删除后强化 C5 Mobile Frontend Independence（避免 `src/` 中残留移动端 UI 代码）

### 1.2 修改的文件

- **`src/lib/icons.ts`**
  - 删除 3 个未使用的图标导出：`CircleDot`（行 14）、`Menu`（行 29）、`MoreVertical`（行 33）
  - 删除前经 Grep 确认在 `src/` 目录（排除 icons.ts 自身）无任何引用
  - 文件从 57 行减至 54 行

- **`src/components/CharacterSidebar.tsx`**
  - 第 1 行 import 补充 `Switch, Match`：`import { Component, For, Show, Switch, Match, createMemo, createSignal } from 'solid-js';`
  - 第 537 行类型窄化：`sectionKey: val` → `sectionKey: val as CharacterBaseSectionKey`
  - 消除 7 条 tsc 错误（6 条 Switch/Match 缺失 + 1 条类型不匹配）

- **`src/components/WorkspaceTransitionStage.tsx`**
  - 第 43 行函数参数 `id` → `_id`（未使用参数，下划线前缀抑制 `noUnusedParameters`）
  - 消除 1 条 tsc 错误（TS6133）

- **`src/components/SchemaConfigPanel.tsx`**
  - 第 329 行类型断言：`objectKind: val` → `objectKind: val as "additional_properties" | "fixed_properties"`
  - 第 400 行类型断言：`type: val` → `type: val as "string" | "number" | "boolean" | "object" | "integer" | "array"`
  - 消除 2 条 tsc 错误（TS2322）

## 2. 改动动机

### 2.1 死代码清理

**问题**：前端存在废弃组件和未使用的导出，增加维护负担。
- `MobileView.tsx` 是 330 行的废弃组件，无任何引用
- `icons.ts` 有 3 个图标导出从未被任何组件使用

**解决**：删除废弃组件和未使用导出，减少 333 行死代码。

### 2.2 tsc 错误修复

**问题**：前端存在 15 条既有 tsc 错误，其中 10 条可快速修复。
- `CharacterSidebar.tsx` 遗漏 `Switch, Match` 导入（6 条错误）
- 类型窄化缺失（3 条错误）
- 未使用参数（1 条错误）

**解决**：修复 10 条可快速修复的错误，剩余 5 条为 App.tsx 跨边界 ID 类型不一致问题，需走 spec 流程。

## 3. 约束合规审计表

| 约束 | 合规 | 说明 |
|------|------|------|
| C1 Frontend Render-Only | √ | 仅前端 UI 代码清理，未触及后端逻辑 |
| C2 Zero-Fallback Errors | √ | 类型断言基于静态选项约束（Select options 为字面量数组），非静默回退 |
| C3 Responsiveness | √ | 删除死代码减少 bundle 体积，提升加载性能 |
| C4 AI UI Isolation | √ | 不涉及 AI 动态 UI 层 |
| C5 Mobile Frontend Independence | √ | 删除 `MobileView.tsx` 强化约束（`src/` 中不再残留移动端 UI 代码）；未触碰 `src-mobile/` |
| C6 Project Cache Location | √ | 不涉及缓存 |
| C7 PC/Android Coverage | √ | 仅 PC 前端清理；移动端独立项目不受影响 |

## 4. 验收记录

### 4.1 构建命令

| 命令 | 工作目录 | Exit Code | 结果 |
|------|----------|-----------|------|
| `npx tsc --noEmit` | `d:\data\Night Voyage` | 2 | **5 个错误**（从 15 减少到 5，减少 10 条），剩余为 App.tsx 跨边界错误 |

### 4.2 错误数对比

| | 清理前 | 清理后 | 变化 |
|------|--------|--------|------|
| tsc 错误数 | 15 | 5 | -10 |
| CharacterSidebar.tsx | 7 | 0 | -7 |
| SchemaConfigPanel.tsx | 2 | 0 | -2 |
| WorkspaceTransitionStage.tsx | 1 | 0 | -1 |
| App.tsx | 5 | 5 | 0（保留，需 spec） |

### 4.3 验收方式

- **MobileView.tsx 删除安全性**：Grep 搜索 `MobileView` 在 `src/` 目录无任何 `import` 引用
- **图标删除安全性**：Grep 搜索每个图标名在 `src/` 目录（排除 icons.ts）无引用
- **Switch/Match 导入修复**：tsc 错误从 7 → 0
- **类型断言安全性**：Select options 均由静态常量生成，值域被约束为目标联合类型子集

### 4.4 预期效果 vs 实际结果

| 预期 | 实际 |
|------|------|
| 删除 330 行废弃组件 | ✓ MobileView.tsx 已删除 |
| 删除 3 个未使用图标 | ✓ CircleDot/Menu/MoreVertical 已删除 |
| 修复 10 条 tsc 错误 | ✓ 错误数 15 → 5 |
| 无新增错误 | ✓ 剩余 5 条均为既有跨边界错误 |
| 未触碰 src-mobile/ | ✓ 确认 |

## 5. 已知限制或后续待办

### 5.1 已知限制

1. **App.tsx 跨边界 ID 类型不一致**（5 条 tsc 错误）：`selectedConversationId: number | null` 与 `WorkspaceTransitionStage` 期望的 `string` 不匹配。根因在后端 Tauri command 返回的会话 ID 类型，需走 spec 流程统一前后端 ID 类型。

2. **类型断言的安全性**：CharacterSidebar.tsx:537 与 SchemaConfigPanel.tsx:329/400 使用类型断言而非运行时校验，依据是 Select options 由静态常量生成。若未来改为动态数据源，需改为运行时校验。

3. **`src/lib/backend.ts` 与 `src/lib/backend/index.ts` 冗余**：两者都是 barrel 文件，本次未处理，需用户确认是否可合并删除其一。

### 5.2 后续待办

1. **App.tsx ID 类型统一**：走 spec 流程统一前后端会话 ID 类型（number vs string）
2. **barrel 文件合并**：确认 `src/lib/backend.ts` 与 `src/lib/backend/index.ts` 是否可合并
3. **移动端移植**：将 CapabilityProfile 体系移植到 `src-mobile/`（capability-matrix-completion spec 的后续待办）

## 6. Anti-False-Completion 自检

| 禁止行为 | 自检结果 |
|----------|----------|
| 禁止 stub/placeholder | ✓ 无 |
| 禁止定值替换 | ✓ 无 |
| 禁止省略错误检查 | ✓ 类型断言基于静态约束 |
| 禁止省略算法步骤 | ✓ 无 |
| 禁止以注释充当实现 | ✓ 无 |
| 禁止 TODO/FIXME 残留 | ✓ 无 |
| 禁止伪造构建/验收结果 | ✓ 真实运行 tsc --noEmit 并贴出 exit code 和错误数 |
