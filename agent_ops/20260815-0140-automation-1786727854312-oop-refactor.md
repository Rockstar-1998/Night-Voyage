# OOP→组合 重构巡逻 · 轮次记录

- **agent_id**: automation-1786727854312
- **轮次时间**: 2026-08-15 01:40 (GMT+8)
- **模式**: 零信任重扫 → 锁定单点 → 组合式重构 → 后门验收 → 记录+提交

## 1. 零信任扫描证据（本轮 Grep 实测）

按任务定义的 5 类模式对 `src-tauri/src/` 全程 Grep（非采信任何 prior 结论）：

| 模式 | Grep 证据 | 判定 |
|------|-----------|------|
| 1. 深层 trait 继承 `trait Sub: Super` | 唯一命中 `services/memory_service.rs:81 pub trait MemoryService: Send + Sync` —— `Send + Sync` 为语言 auto marker trait，非行为继承链 | 非违规（误报） |
| 2. downcast 模拟 | `as_any` / `downcast` 全仓 0 命中 | 无 |
| 3. `enum Kind + bool flag` | `enum PromptBlockKind`(prompt_compiler.rs:84)、`PresetBlockValidationKind`(346)、`WorldBookTriggerSourceKind`(world_book_matcher.rs:4) 均为纯数据枚举，配对的 `is_constant`/`required` 字段仅用于排序/过滤/缓存键，未与枚举组合做变体行为分发 | 无 |
| 4. 行为 bool 标志位 | `enabled`/`is_enabled`/`auto_retry_enabled` 等全部为 DB 持久化列或 IPC command 参数；改为类型状态将破坏存储格式/IPC 契约（违反铁律「不改变存储格式/IPC契约」） | 不可作为安全重构目标 |
| 5. `_ =>` / `..` 吞变体 | 绝大多数 `_ =>` 为外部字符串解析的 `Err(...)` 返回（符合 C2 零回退）；内部枚举（PromptBlockKind 等）均穷尽匹配无 `_ =>`。唯一吞变体：`PromptRole::from_message_role` 的 `_ => Self::User` | **命中 1 处** |

## 2. 本轮锁定并重构的违规点

- **模式 #5 `_ =>` 吞未知变体**（兼 C2 零回退违规）
- **文件/位置**: `src-tauri/src/services/prompt_compiler.rs`
  - 旧（74-80）：`fn from_message_role(role: &str) -> Self` 用 `_ => Self::User` 把任意未知/损坏的 role 字符串静默回退为 `User`。
  - 新（74-82）：改为 `fn from_message_role(role: &str) -> Result<Self, String>`，显式穷尽 `system/assistant/user`，未知值返回 `Err(...)`，由调用方 `?` 传播为可见错误。
- **调用点更新**（均加 `?`，函数本身返回 `Result<_, String>`，与既有 `.map_err(|e| e.to_string())?` 错误风格一致）：
  - `prompt_compiler.rs:2069`（原 2066）
  - `prompt_compiler.rs:2189`（原 2186）
  - `prompt_compiler.rs:2258`（原 2255）
- 调用点处 `row.try_get("role").unwrap_or_else(|_| "user"/"assistant")` 仅处理「DB 列缺失」默认，与 `from_message_role` 处理「列存在但值损坏」职责分离，互不冲突。

## 3. 后门验收（真实构建证据）

命令：
```
export PATH="/d/data/Night Voyage/.cache/cargo/bin:$PATH" && cd src-tauri && CARGO_TARGET_X86_64_PC_WINDOWS_MSVC_RUSTFLAGS="" cargo build 2>&1 | tail -40
```
结果：**退出码 0**，末段输出 `Finished 'dev' profile [optimized + debuginfo] target(s) in 3m 36s`。
仅有 8 条 pre-existing dead-code 警告（`preset_gate_repository.rs` 未使用函数、`BlueprintError` 未构造变体等），与本次改动无关，非 error。

## 4. 是否终止循环

否。仍有 1 处同类 `_ =>` 吞变体遗留待后续轮次处理：`llm/mod.rs:199` `impl From<ChatMessage> for LlmMessage` 中的 `_ => LlmRole::User`（位于 `From` trait 实现内，需改为 `TryFrom` 并调整 `.into()` 调用点，跨文件涟漪较大，留待下一轮单独处理）。其余 `_ =>` 均为合法 `Err` 返回或外部输入解析，不构成违规。
